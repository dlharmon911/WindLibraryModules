module wind;

import <cstdint>;
import :base;
import :map;

namespace wind
{
	namespace map
	{
		bool load(map_t& map, const string_t& filename)
		{
			return false;
		}

		bool save(const map_t& map, const string_t& filename)
		{
			return false;
		}
	}
}
